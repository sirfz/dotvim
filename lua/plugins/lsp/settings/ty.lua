return {
    cmd = { 'uvx', 'ty', 'server' },
    settings = {
        ty = {
            experimental = {
                autoImport = true,
                rename = true,
            },
        },
    }
}
